var getType = function (type) {
  if (type === 'radio' || type === 'radio_source') {
    return '(单选)'
  } else if (type === 'multiple_source' || type === 'multiple') {
    return '(多选)'
  } else {
    return ''
  }
}

module.exports = {
  getType: getType,
}